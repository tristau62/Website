<%= form_for([@location, @location.notes.build]) do |f| %>
  <p>
    <%= f.label :owner %><br>
    <%= f.text_field :owner %>
  </p>
  <p>
    <%= f.label :body %><br>
    <%= f.text_area :body %>
  </p>
  <p>
    <%= f.submit %>
  </p>
<% end %>